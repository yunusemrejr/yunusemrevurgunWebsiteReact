import React from "react";
import Header from "../components/Header";
import Footer from "../components/Footer";

const HomePage = () => {
    return (
        <div>
            <Header />
            <h2 id="h2home">Welcome to Yunus's Website</h2><br>
            </br><p id="lastp">Never fear what is to come. Instead, fear what you are missing at the moment by thinking about that future catastrophe. Now go back to coding.</p>
            <Footer />

        </div>
    );
};

export default HomePage;